import { isObject_ } from "./Utils/TypeUtils";
import AppConfig from './AppConfig';

/**
 * This file exists as the main entry point to execute within the Google Apps Script UI. entry is simply an example and can be replaced.
 */
function entry() {
}