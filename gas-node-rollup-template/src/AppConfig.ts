
const AppConfig = {
  // All properties accessible in google under Project Settings > Scripts Properties.
  tokens: {
      //configuration_id: PropertiesService.getScriptProperties().getProperty('configuration_id')!
  }
};

export default AppConfig;